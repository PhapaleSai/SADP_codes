

    # 1) Run some python programs on Pi like:  
    # a) Read your name and print Hello message with name 
    # b) Read two numbers and print their sum, difference, product and division.  
    # c) Word and character count of a given string.  
    # d) Area of a given shape (rectangle, triangle and circle) reading shape and appropriate     
    # values from standard input

    # a) Read your name and print Hello message with name 
    name = input("Enter your name")
    print("Hello " + name+"!")

    Enter your namesai
    Hello sai!

    # b) Read two numbers and print their sum, difference, product and division.  
    num1 = float(input("Enter 1st number:"))
    num2 = float(input("Enter 2nd number:"))

    print("Sum is :", num1 + num2)
    print("Differnce is :", num1 - num2)
    print("Product is :", num1 * num2)

    if num2 == 0:
        print("Division: Cannot divide by zero")
    else:
            print("Division is :" , num1 /num2)

    Enter 1st number:10
    Enter 2nd number:5
    Sum is : 15.0
    Differnce is : 5.0
    Product is : 50.0
    Division is : 2.0

    # c) Word and character count of a given string.

    text = input("Enter a string:")

    word_count = len(text.split())
    char_count = len(text)

    print("Word Count is :",word_count)
    print("Character Count is ",char_count)

    Enter a string:i am sai
    Word Count is : 3
    Character Count is  8

    #d) Area of a given shape (rectangle, triangle and circle) reading shape and appropriate values from standard input

    shape = input("Enter the shape (rectangle/triangle/circle): ").lower() 
    if shape == "rectangle": 
        length = float(input("Enter length: ")) 
        width = float(input("Enter width: ")) 
        area = length * width 

    elif shape == "triangle": 
        base = float(input("Enter base: ")) 
        height = float(input("Enter height: "))
        area = 0.5 * base * height 

    elif shape == "circle": 
        radius = float(input("Enter radius: ")) 
        area = 3.14159 * radius * radius 
    else: 
        area = None 
        print("Invalid shape!") 

    if area is not None: 
        print("Area of the", shape, "is:", area)

    Enter the shape (rectangle/triangle/circle): rectangle
    Enter length: 4
    Enter width: 4
    Area of the rectangle is: 16.0

    # 2) Run some python programs on Pi like:  
    # a) Print a name 'n' times, where name and n are read from standard input, using for and  while loops.  
    # b) Handle Divided by Zero Exception.  
    # c) Print current time for 10 times with an interval of 10 seconds.  
    # d) Read a fileline byline and print the word count of each line. 

    # a) Print a name 'n' times, where name and n are read from standard input, using for and  while loops.  

    name = input("Enter your name")
    n = int(input("How many time do you want to print your name"))
    print("First Using for loop")
    for i in range(n):
        print(name)
        
    print("Using While loop")
    count = 0
    while count < n:
            print(name)
            count += 1

    Enter your namesai
    How many time do you want to print your name4
    First Using for loop
    sai
    sai
    sai
    sai
    Using While loop
    sai
    sai
    sai
    sai

    # b) Handle Divided by Zero Exception. 

    try:
        num1 = float(input("Enter 1st number:"))
        num2 = float(input("Enter 2nd number:"))
        result = num1 / num2
        print("Result is :",result)
    except ZeroDivisionError:
        print("Error Canot divide by 0")

    Enter 1st number:4
    Enter 2nd number:2
    Result is : 2.0

    # c) Print current time for 10 times with an interval of 10 seconds.  
    import time
    from datetime import datetime

    for i in range(10):
        current_time = datetime.now().strftime('%H:%M:%S')
        print("Current time is :",current_time)
        time.sleep(10)
        

    Current time is : 20:12:31
    Current time is : 20:12:41
    Current time is : 20:12:51
    Current time is : 20:13:01
    Current time is : 20:13:11
    Current time is : 20:13:21
    Current time is : 20:13:31
    Current time is : 20:13:41
    Current time is : 20:13:51
    Current time is : 20:14:01

    # d) Read a fileline byline and print the word count of each line.
    filename = "sample.txt"

    with open(filename,"r") as file:
        line_number = 1
        for line in file:
            words = line.split()
            count = len(words)
            print(f"Line {line_number} has {count} words.") 
            line_number += 1 

    Line 1 has 4 words.
    Line 2 has 5 words.
    Line 3 has 5 words.
--------------------------------------------------------------------
🖥️ Raspberry Pi Linux Commands – Practical Worksheet
🎯 Objective
Learn and practice basic Linux commands on Raspberry Pi (Raspbian OS) using the Terminal.
Understand how files, folders, and processes are managed using the Command Line Interface (CLI).

🪄 How to Start
Boot up your Raspberry Pi.
Open the Terminal (black icon) or press Ctrl + Alt + T.
You’ll see a prompt like:
pi@raspberrypi:~ $
Type each command and press Enter to execute.
🧠 Commands, Examples & Explanations
1️⃣ ls – List Files and Folders
Use: Displays files and folders in the current directory.
Syntax:

ls [options]
Examples:

ls
ls -l
ls -a
Explanation:

ls → lists files/folders.
ls -l → detailed info (permissions, owner, size, date).
ls -a → includes hidden files.
2️⃣ cd – Change Directory
Use: Move between folders.
Syntax:

cd [foldername]
Examples:

cd /
cd ~
cd /home/pi/Desktop
Explanation: Navigates between directories. ~ represents your home directory.

3️⃣ touch – Create an Empty File
Use: Creates a new blank file.
Syntax:

touch [filename]
Example:

touch notes.txt
ls
Explanation: Creates a new empty file named notes.txt.

4️⃣ mv – Move or Rename Files
Use: Moves or renames files.
Syntax:

mv [source] [destination]
Examples:

mv notes.txt /home/pi/Documents
mv notes.txt mynotes.txt
Explanation: Used for moving files or renaming them.

5️⃣ rm – Remove Files or Folders
Use: Deletes files or folders permanently.
Syntax:

rm [filename]
Examples:

rm old.txt
rm -r myfolder
Explanation: Removes files or directories (-r means recursive).
⚠️ Use with caution — deleted files cannot be recovered.

6️⃣ man – Manual (Help Command)
Use: Displays detailed help for a command.
Syntax:

man [command]
Example:

man ls
Explanation: Shows manual pages for commands. Press q to exit.

7️⃣ mkdir – Make Directory
Use: Creates a new folder.
Syntax:

mkdir [foldername]
Example:

mkdir Projects
ls
Explanation: Creates a directory called Projects.

8️⃣ rmdir – Remove Empty Directory
Use: Deletes an empty folder.
Syntax:

rmdir [foldername]
Example:

rmdir Projects
Explanation: Removes only empty directories.

9️⃣ tar – Archive Files
Use: Combines multiple files into one archive.
Syntax:

tar -cvf [archive.tar] [foldername]
tar -xvf [archive.tar]
Examples:

tar -cvf backup.tar Documents
tar -xvf backup.tar
Explanation:
-c create, -v verbose, -f file name, -x extract.

🔟 gzip – Compress Files
Use: Compresses or decompresses files.
Syntax:

gzip [filename]
gunzip [filename.gz]
Examples:

gzip notes.txt
gunzip notes.txt.gz
Explanation:
gzip compresses and gunzip decompresses files.

1️⃣1️⃣ cat – View File Content
Use: Displays file content on the screen.
Syntax:

cat [filename]
Example:

cat notes.txt
Explanation: Displays the full contents of a file.

1️⃣2️⃣ more – View File Page by Page
Use: Shows large file content one screen at a time.
Syntax:

more [filename]
Example:

more longfile.txt
Explanation: Press Spacebar for the next page, q to quit.

1️⃣3️⃣ less – Scroll Through File
Use: Similar to more but allows both up and down scrolling.
Syntax:

less [filename]
Example:

less log.txt
Explanation: Use ↑ and ↓ to scroll, q to exit.

1️⃣4️⃣ ps – Process Status
Use: Lists currently running processes.
Syntax:

ps
Example:

ps
Explanation: Displays process IDs (PID) and terminal sessions.

1️⃣5️⃣ sudo – Superuser Command
Use: Runs a command as the administrator (root user).
Syntax:

sudo [command]
Example:

sudo ls /root
Explanation: Grants admin rights for restricted tasks.

1️⃣6️⃣ cron – Task Scheduler
Use: Runs commands automatically at set times.
Syntax:

crontab -e
Example Entry:

* * * * * echo "Hello Pi" >> /home/pi/cronlog.txt
Explanation: Runs every minute, adding "Hello Pi" to the file cronlog.txt.

1️⃣7️⃣ chown – Change File Owner
Use: Changes who owns a file.
Syntax:

sudo chown [user]:[group] [file]
Example:

sudo chown pi:pi notes.txt
Explanation: Transfers ownership of a file to a specific user/group.

1️⃣8️⃣ chgrp – Change Group Ownership
Use: Changes the group that owns a file.
Syntax:

sudo chgrp [group] [file]
Example:

sudo chgrp admin notes.txt
Explanation: Assigns file to a group (e.g., admin, users).

1️⃣9️⃣ ping – Test Internet Connection
Use: Tests if a network or website is reachable.
Syntax:

ping [hostname or IP]
Example:

ping google.com
Explanation: Sends data packets to check connectivity.
Press Ctrl + C to stop.
